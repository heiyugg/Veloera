[33mtag v0.5.0[m
Tagger: TethysPlex <180962954+TethysPlex@users.noreply.github.com>
Date:   Tue Sep 2 15:47:04 2025 +1000

release version 0.5.0

[33mcommit a0507ef014ca721029861e5cae7e22d541299cb4[m[33m ([m[1;33mtag: [m[1;33mv0.5.0[m[33m, [m[1;31mupstream/main[m[33m, [m[1;31mupstream/HEAD[m[33m)[m
Author: AAEE86 <33052466+AAEE86@users.noreply.github.com>
Date:   Tue Sep 2 12:26:06 2025 +0800

    feat(model-mapping): 实现模型映射实时同步与编辑优化 (#197)
    
    * feat(model-mapping): 实现模型映射实时同步与编辑优化
    
    - 在 ModelMappingEditor 组件添加 onRealtimeChange 属性，实现实时同步映射数据
    - 通过内部标记避免循环更新，优化父组件与子组件之间的数据交互
    - 修改映射对时支持实时推送但不立即更新父组件状态，失焦后统一同步
    - 实现模式切换时模板填充后立即同步映射数据及触发实时回调
    - 优化 EditChannel 组件中模型列表映射应用逻辑，支持恢复原始模型名称
    - 使用 set 集合避免模型列表重复，并提升映射关系的处理效率
    - 编辑模型映射时，新增和删除映射都实时同步模型配置用于实时预览
    - 修改模型列表更新方式，防止重复模型名，同时改进内部状态管理
    - 细化界面交互体验，完善输入框失焦事件与映射同步逻辑
    - 优化代码结构和注释，提升可读性及维护便利性
    
    * fix: 自动删除原始模型导致的问题，由用户手动删除

[1mdiff --git a/web/src/pages/Channel/EditChannel.js b/web/src/pages/Channel/EditChannel.js[m
[1mindex d22c2b85..27f684fe 100644[m
[1m--- a/web/src/pages/Channel/EditChannel.js[m
[1m+++ b/web/src/pages/Channel/EditChannel.js[m
[36m@@ -16,7 +16,7 @@[m [mGNU General Public License for more details.[m
 You should have received a copy of the GNU General Public License[m
 along with this program. If not, see <https://www.gnu.org/licenses/>.[m
 */[m
[31m-import React, { useEffect, useRef, useState, useMemo } from 'react';[m
[32m+[m[32mimport React, { useEffect, useRef, useState, useMemo, useCallback } from 'react';[m
 import { useNavigate, useParams } from 'react-router-dom';[m
 import { useTranslation } from 'react-i18next';[m
 import {[m
[36m@@ -239,13 +239,16 @@[m [mconst MODEL_MAPPING_EXAMPLE = {[m
 };[m
 [m
 // ModelMappingEditor component for visual key-value editing[m
[31m-const ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
[32m+[m[32mconst ModelMappingEditor = ({ value, onChange, placeholder, onRealtimeChange }) => {[m
   const { t } = useTranslation();[m
   const [mappingPairs, setMappingPairs] = useState([]);[m
   const [mode, setMode] = useState('visual'); // 'visual' or 'json'[m
   const [jsonValue, setJsonValue] = useState('');[m
   const [jsonError, setJsonError] = useState('');[m
 [m
[32m+[m[32m  // 用于标记是否是内部更新，避免循环更新[m
[32m+[m[32m  const isInternalUpdateRef = useRef(false);[m
[32m+[m
   // Parse JSON value to key-value pairs[m
   const parseJsonToMappings = (jsonStr) => {[m
     if (!jsonStr || jsonStr.trim() === '') {[m
[36m@@ -254,10 +257,13 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
     try {[m
       const parsed = JSON.parse(jsonStr);[m
       if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {[m
[31m-        return Object.entries(parsed).map(([key, value]) => ({ [m
[31m-          id: Date.now() + Math.random(), [m
[31m-          key, [m
[31m-          value [m
[32m+[m[32m        const unsafe = new Set(['__proto__', 'prototype', 'constructor']);[m
[32m+[m[32m        return Object.entries(parsed)[m
[32m+[m[32m          .filter(([k]) => !unsafe.has(k))[m
[32m+[m[32m          .map(([key, value]) => ({[m
[32m+[m[32m          id: Date.now() + Math.random(),[m
[32m+[m[32m          key,[m
[32m+[m[32m          value[m
         }));[m
       }[m
       return [];[m
[36m@@ -271,10 +277,12 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
     if (!pairs || pairs.length === 0) {[m
       return '';[m
     }[m
[31m-    const obj = {};[m
[32m+[m[32m    const obj = Object.create(null);[m
[32m+[m[32m    const unsafe = new Set(['__proto__', 'prototype', 'constructor']);[m
     pairs.forEach(pair => {[m
       if (pair.key && pair.key.trim() !== '') {[m
[31m-        obj[pair.key.trim()] = pair.value || '';[m
[32m+[m[32m        const k = pair.key.trim();[m
[32m+[m[32m        if (!unsafe.has(k)) obj[k] = pair.value || '';[m
       }[m
     });[m
     return Object.keys(obj).length > 0 ? JSON.stringify(obj, null, 2) : '';[m
[36m@@ -282,10 +290,14 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
 [m
   // Initialize component state from value prop[m
   useEffect(() => {[m
[31m-    const pairs = parseJsonToMappings(value);[m
[31m-    setMappingPairs(pairs.length > 0 ? pairs : [{ id: Date.now() + Math.random(), key: '', value: '' }]);[m
[31m-    setJsonValue(value || '');[m
[31m-    setJsonError('');[m
[32m+[m[32m    // 只有在非内部更新时才同步外部状态[m
[32m+[m[32m    if (!isInternalUpdateRef.current) {[m
[32m+[m[32m      const pairs = parseJsonToMappings(value);[m
[32m+[m[32m      setMappingPairs(pairs.length > 0 ? pairs : [{ id: Date.now() + Math.random(), key: '', value: '' }]);[m
[32m+[m[32m      setJsonValue(value || '');[m
[32m+[m[32m      setJsonError('');[m
[32m+[m[32m    }[m
[32m+[m[32m    isInternalUpdateRef.current = false;[m
   }, [value]);[m
 [m
   // Add new mapping pair[m
[36m@@ -299,19 +311,35 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
     const newPairs = mappingPairs.filter((_, i) => i !== index);[m
     const finalPairs = newPairs.length > 0 ? newPairs : [{ id: Date.now() + Math.random(), key: '', value: '' }];[m
     setMappingPairs(finalPairs);[m
[31m-    // Update parent with new JSON[m
[32m+[m
[32m+[m[32m    // 立即更新父组件[m
     const jsonStr = mappingsToJson(finalPairs);[m
[32m+[m[32m    isInternalUpdateRef.current = true;[m
     onChange(jsonStr);[m
[32m+[m
[32m+[m[32m    // 触发实时同步[m
[32m+[m[32m    if (onRealtimeChange) {[m
[32m+[m[32m      onRealtimeChange(jsonStr);[m
[32m+[m[32m    }[m
   };[m
 [m
[31m-  // Update mapping pair[m
[32m+[m[32m  // Update mapping pair - 只更新本地状态，不触发父组件更新[m
   const updateMappingPair = (index, field, value) => {[m
     const newPairs = [...mappingPairs];[m
     newPairs[index] = { ...newPairs[index], [field]: value };[m
     setMappingPairs(newPairs);[m
[31m-    [m
[31m-    // Update parent with new JSON[m
[31m-    const jsonStr = mappingsToJson(newPairs);[m
[32m+[m
[32m+[m[32m    // 触发实时同步回调，但不更新父组件的 model_mapping 状态[m
[32m+[m[32m    if (onRealtimeChange) {[m
[32m+[m[32m      const jsonStr = mappingsToJson(newPairs);[m
[32m+[m[32m      onRealtimeChange(jsonStr);[m
[32m+[m[32m    }[m
[32m+[m[32m  };[m
[32m+[m
[32m+[m[32m  // 在失去焦点时同步到父组件[m
[32m+[m[32m  const handleInputBlur = () => {[m
[32m+[m[32m    const jsonStr = mappingsToJson(mappingPairs);[m
[32m+[m[32m    isInternalUpdateRef.current = true;[m
     onChange(jsonStr);[m
   };[m
 [m
[36m@@ -331,10 +359,13 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
         } else {[m
           const parsed = JSON.parse(jsonValue);[m
           if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {[m
[31m-            const pairs = Object.entries(parsed).map(([key, value]) => ({ [m
[31m-              id: Date.now() + Math.random(), [m
[31m-              key, [m
[31m-              value [m
[32m+[m[32m            const unsafe = new Set(['__proto__', 'prototype', 'constructor']);[m
[32m+[m[32m              const pairs = Object.entries(parsed)[m
[32m+[m[32m                .filter(([k]) => !unsafe.has(k))[m
[32m+[m[32m                .map(([key, value]) => ({[m
[32m+[m[32m                id: Date.now() + Math.random(),[m
[32m+[m[32m                key,[m
[32m+[m[32m                value[m
             }));[m
             setMappingPairs(pairs.length > 0 ? pairs : [{ id: Date.now() + Math.random(), key: '', value: '' }]);[m
             setJsonError('');[m
[36m@@ -354,19 +385,25 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
   // Handle JSON input change[m
   const handleJsonChange = (newValue) => {[m
     setJsonValue(newValue);[m
[31m-    [m
[31m-    // Validate JSON and update parent[m
[32m+[m
[32m+[m[32m    // Validate JSON[m
     if (newValue.trim() === '') {[m
       setJsonError('');[m
[31m-      onChange('');[m
[32m+[m[32m      // 触发实时同步[m
[32m+[m[32m      if (onRealtimeChange) {[m
[32m+[m[32m        onRealtimeChange('');[m
[32m+[m[32m      }[m
       return;[m
     }[m
[31m-    [m
[32m+[m
     try {[m
       const parsed = JSON.parse(newValue);[m
       if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {[m
         setJsonError('');[m
[31m-        onChange(newValue);[m
[32m+[m[32m        // 触发实时同步[m
[32m+[m[32m        if (onRealtimeChange) {[m
[32m+[m[32m          onRealtimeChange(newValue);[m
[32m+[m[32m        }[m
       } else {[m
         setJsonError(t('请输入有效的JSON对象格式'));[m
       }[m
[36m@@ -375,16 +412,39 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
     }[m
   };[m
 [m
[32m+[m[32m  // 在 JSON 模式失去焦点时同步到父组件[m
[32m+[m[32m  const handleJsonBlur = () => {[m
[32m+[m[32m    if (!jsonError && jsonValue.trim() !== '') {[m
[32m+[m[32m      try {[m
[32m+[m[32m        const parsed = JSON.parse(jsonValue);[m
[32m+[m[32m        if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {[m
[32m+[m[32m          isInternalUpdateRef.current = true;[m
[32m+[m[32m          onChange(jsonValue);[m
[32m+[m[32m        }[m
[32m+[m[32m      } catch (error) {[m
[32m+[m[32m        // 忽略错误，不更新父组件[m
[32m+[m[32m      }[m
[32m+[m[32m    } else if (jsonValue.trim() === '') {[m
[32m+[m[32m      isInternalUpdateRef.current = true;[m
[32m+[m[32m      onChange('');[m
[32m+[m[32m    }[m
[32m+[m[32m  };[m
[32m+[m
   // Fill template[m
   const fillTemplate = () => {[m
     const templateJson = JSON.stringify(MODEL_MAPPING_EXAMPLE, null, 2);[m
     if (mode === 'visual') {[m
       const pairs = parseJsonToMappings(templateJson);[m
       setMappingPairs(pairs);[m
[31m-      onChange(templateJson);[m
     } else {[m
       setJsonValue(templateJson);[m
[31m-      handleJsonChange(templateJson);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    // 立即更新父组件和触发实时同步[m
[32m+[m[32m    isInternalUpdateRef.current = true;[m
[32m+[m[32m    onChange(templateJson);[m
[32m+[m[32m    if (onRealtimeChange) {[m
[32m+[m[32m      onRealtimeChange(templateJson);[m
     }[m
   };[m
 [m
[36m@@ -395,7 +455,7 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
           <Button[m
             type={mode === 'visual' ? 'primary' : 'tertiary'}[m
             onClick={() => switchMode('visual')}[m
[31m-            style={{ [m
[32m+[m[32m            style={{[m
               borderRadius: '6px 0 0 6px'[m
             }}[m
           >[m
[36m@@ -404,7 +464,7 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
           <Button[m
             type={mode === 'json' ? 'primary' : 'tertiary'}[m
             onClick={() => switchMode('json')}[m
[31m-            style={{ [m
[32m+[m[32m            style={{[m
               borderRadius: '0 6px 6px 0'[m
             }}[m
           >[m
[36m@@ -428,13 +488,14 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
           <div style={{ marginBottom: 10 }}>[m
             <Typography.Text type="secondary">{placeholder}</Typography.Text>[m
           </div>[m
[31m-          [m
[32m+[m
           {mappingPairs.map((pair, index) => ([m
             <div key={pair.id} style={{ display: 'flex', alignItems: 'center', marginBottom: 8 }}>[m
               <Input[m
                 placeholder={t('目标模型名称')}[m
                 value={pair.key}[m
                 onChange={(value) => updateMappingPair(index, 'key', value)}[m
[32m+[m[32m                onBlur={() => handleInputBlur(index, 'key')}[m
                 style={{ flex: 1, marginRight: 8 }}[m
               />[m
               <Typography.Text style={{ margin: '0 8px' }}>→</Typography.Text>[m
[36m@@ -442,6 +503,7 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
                 placeholder={t('实际模型名称')}[m
                 value={pair.value}[m
                 onChange={(value) => updateMappingPair(index, 'value', value)}[m
[32m+[m[32m                onBlur={() => handleInputBlur(index, 'value')}[m
                 style={{ flex: 1, marginRight: 8 }}[m
               />[m
               <Button[m
[36m@@ -453,7 +515,7 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
               />[m
             </div>[m
           ))}[m
[31m-          [m
[32m+[m
           <Button[m
             type="tertiary"[m
             icon={<IconPlusCircle />}[m
[36m@@ -473,6 +535,7 @@[m [mconst ModelMappingEditor = ({ value, onChange, placeholder }) => {[m
             }[m
             value={jsonValue}[m
             onChange={handleJsonChange}[m
[32m+[m[32m            onBlur={handleJsonBlur}[m
             autosize[m
             autoComplete='new-password'[m
           />[m
[36m@@ -586,7 +649,7 @@[m [mconst EditChannel = (props) => {[m
     if (!mappingValue || typeof mappingValue !== 'string' || mappingValue.trim() === '') {[m
       return null;[m
     }[m
[31m-    [m
[32m+[m
     try {[m
       const mapping = JSON.parse(mappingValue);[m
       if (typeof mapping !== 'object' || mapping === null) {[m
[36m@@ -607,98 +670,102 @@[m [mconst EditChannel = (props) => {[m
   // 更新模型列表的统一方法[m
   const updateModelsList = (newModels, newMapping) => {[m
     const uniqueModels = Array.from(new Set(newModels.filter(model => model && model.trim())));[m
[31m-    [m
[31m-    setInputs((inputs) => ({ ...inputs, models: uniqueModels }));[m
[31m-    setModelOriginalMapping(newMapping);[m
[31m-  };[m
 [m
[31m-  // 恢复模型到原始名称[m
[31m-  const restoreModelsToOriginalNames = () => {[m
[31m-    const currentModels = getCurrentModels();[m
[31m-    const restoredModels = currentModels.map(model => modelOriginalMapping[model] || model);[m
[31m-    [m
[31m-    // 使用数组比较而不是JSON.stringify提高性能[m
[31m-    const hasChanges = currentModels.length !== restoredModels.length || [m
[31m-      currentModels.some((model, index) => model !== restoredModels[index]);[m
[31m-    [m
[31m-    if (hasChanges) {[m
[31m-      updateModelsList(restoredModels, {});[m
[31m-    }[m
[32m+[m[32m    setInputs((prevInputs) => ({ ...prevInputs, models: uniqueModels }));[m
[32m+[m[32m    setModelOriginalMapping(newMapping);[m
   };[m
 [m
   // 应用模型映射的核心逻辑[m
   const applyModelMapping = (mapping, currentModels, currentMapping) => {[m
[31m-    let updatedModels = [...currentModels];[m
[31m-    let newMapping = { ...currentMapping };[m
[32m+[m[32m    if (!mapping || typeof mapping !== 'object') {[m
[32m+[m[32m      return { updatedModels: currentModels, newMapping: {}, hasChanges: false };[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    const updatedModels = new Set(currentModels);[m
[32m+[m[32m    const newMapping = Object.create(null);[m
     let hasChanges = false;[m
 [m
[31m-    // 遍历重定向映射[m
[31m-    Object.entries(mapping).forEach(([key, mappedValue]) => {[m
[31m-      if (typeof key === 'string' && typeof mappedValue === 'string') {[m
[31m-        const keyTrimmed = key.trim();[m
[31m-        const valueTrimmed = mappedValue.trim();[m
[31m-[m
[31m-        if (keyTrimmed && valueTrimmed) {[m
[31m-          // 查找模型配置中是否存在重定向的"值"（原始模型名）[m
[31m-          const valueIndex = updatedModels.findIndex(model => {[m
[31m-            return model === valueTrimmed || newMapping[model] === valueTrimmed;[m
[31m-          });[m
[31m-[m
[31m-          if (valueIndex !== -1) {[m
[31m-            const currentDisplayName = updatedModels[valueIndex];[m
[31m-            if (currentDisplayName !== keyTrimmed) {[m
[31m-              // 记录原始映射关系[m
[31m-              if (!newMapping[keyTrimmed]) {[m
[31m-                newMapping[keyTrimmed] = newMapping[currentDisplayName] || currentDisplayName;[m
[31m-              }[m
[31m-              // 清理旧的映射关系[m
[31m-              if (newMapping[currentDisplayName]) {[m
[31m-                delete newMapping[currentDisplayName];[m
[31m-              }[m
[31m-              // 更新显示名称为重定向的键[m
[31m-              updatedModels[valueIndex] = keyTrimmed;[m
[31m-              hasChanges = true;[m
[31m-            }[m
[31m-          }[m
[32m+[m[32m    // 处理新的映射关系[m
[32m+[m[32m    Object.entries(mapping).forEach(([displayName, originalName]) => {[m
[32m+[m[32m      const displayNameTrimmed = displayName.trim();[m
[32m+[m[32m      const originalNameTrimmed = originalName?.trim();[m
[32m+[m
[32m+[m[32m      if (displayNameTrimmed && originalNameTrimmed) {[m
[32m+[m[32m        // 添加显示名称到模型列表中（如果不存在）[m
[32m+[m[32m        if (!updatedModels.has(displayNameTrimmed)) {[m
[32m+[m[32m          updatedModels.add(displayNameTrimmed);[m
[32m+[m[32m          hasChanges = true;[m
         }[m
[31m-      }[m
[31m-    });[m
 [m
[31m-    // 处理不在映射中的模型，恢复为原始名称[m
[31m-    const mappingKeys = new Set(Object.keys(mapping).map(key => key.trim()));[m
[31m-    updatedModels = updatedModels.map(model => {[m
[31m-      if (!mappingKeys.has(model) && newMapping[model]) {[m
[31m-        const originalName = newMapping[model];[m
[31m-        delete newMapping[model];[m
[31m-        hasChanges = true;[m
[31m-        return originalName;[m
[32m+[m[32m        // 建立映射关系：displayName -> originalName[m
[32m+[m[32m        newMapping[displayNameTrimmed] = originalNameTrimmed;[m
       }[m
[31m-      return model;[m
     });[m
 [m
[31m-    return { updatedModels, newMapping, hasChanges };[m
[32m+[m[32m    const result = {[m
[32m+[m[32m      updatedModels: Array.from(updatedModels),[m
[32m+[m[32m      newMapping,[m
[32m+[m[32m      hasChanges[m
[32m+[m[32m    };[m
[32m+[m[32m    return result;[m
   };[m
 [m
[31m-  // 实时同步模型重定向到模型配置的函数[m
[31m-  const syncModelMappingToModels = (mappingValue) => {[m
[32m+[m[32m  // 实时同步模型重定向到模型配置的函数（用于实时预览）[m
[32m+[m[32m  const syncModelMappingToModels = useCallback((mappingValue) => {[m
     const mapping = parseModelMapping(mappingValue);[m
[31m-    [m
[31m-    if (!mapping) {[m
[31m-      restoreModelsToOriginalNames();[m
[32m+[m
[32m+[m[32m    if (!mapping || Object.keys(mapping).length === 0) {[m
[32m+[m[32m      // 当映射为空时，恢复为原始名称[m
[32m+[m[32m      const currentModels = getCurrentModels();[m
[32m+[m
[32m+[m[32m      // 模型列表为空，则不需要恢复[m
[32m+[m[32m      if (!currentModels || currentModels.length === 0) {[m
[32m+[m[32m        return;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      // 没有映射关系，则不需要恢复[m
[32m+[m[32m      if (!modelOriginalMapping || Object.keys(modelOriginalMapping).length === 0) {[m
[32m+[m[32m        updateModelsList(currentModels, {});[m
[32m+[m[32m        return;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      // 当删除映射时，需要将显示名称（key）替换为原始名称（value）[m
[32m+[m[32m      const restoredModels = currentModels.map(model => {[m
[32m+[m[32m        if (modelOriginalMapping[model]) {[m
[32m+[m[32m          return modelOriginalMapping[model];[m
[32m+[m[32m        }[m
[32m+[m[32m        return model;[m
[32m+[m[32m      });[m
[32m+[m
[32m+[m[32m      // 去重[m
[32m+[m[32m      const uniqueRestoredModels = Array.from(new Set(restoredModels));[m
[32m+[m
[32m+[m[32m      // 清空原始映射关系，因为已经恢复完成[m
[32m+[m[32m      updateModelsList(uniqueRestoredModels, {});[m
       return;[m
     }[m
 [m
     const currentModels = getCurrentModels();[m
[32m+[m
[32m+[m[32m    // 在应用新映射之前，先恢复当前模型到原始名称[m
[32m+[m[32m    const restoredModels = currentModels.map(model => {[m
[32m+[m[32m      // 如果模型在原始映射中，恢复为原始名称[m
[32m+[m[32m      if (modelOriginalMapping[model]) {[m
[32m+[m[32m        return modelOriginalMapping[model];[m
[32m+[m[32m      }[m
[32m+[m[32m      return model;[m
[32m+[m[32m    });[m
     const { updatedModels, newMapping, hasChanges } = applyModelMapping([m
[31m-      mapping, [m
[31m-      currentModels, [m
[31m-      modelOriginalMapping[m
[32m+[m[32m      mapping,[m
[32m+[m[32m      restoredModels,[m
[32m+[m[32m      {}[m
     );[m
 [m
[31m-    if (hasChanges) {[m
[31m-      updateModelsList(updatedModels, newMapping);[m
[32m+[m[32m      if (hasChanges || JSON.stringify([...currentModels].sort()) !== JSON.stringify([...updatedModels].sort())) {[m
[32m+[m[32m        updateModelsList(updatedModels, newMapping);[m
     }[m
[31m-  };[m
[32m+[m[32m  }, [modelOriginalMapping, inputs.models]);[m
[32m+[m
 [m
   // Handle changes to the key list[m
   const updateKeyListToInput = (newKeyList) => {[m
[36m@@ -760,10 +827,9 @@[m [mconst EditChannel = (props) => {[m
       return;[m
     }[m
 [m
[31m-    // 处理模型重定向变更时自动同步模型配置（实时同步）[m
[32m+[m[32m    // 处理模型重定向变更时只更新状态，不触发实时同步[m
     if (name === 'model_mapping') {[m
       setInputs((inputs) => ({ ...inputs, [name]: value }));[m
[31m-      syncModelMappingToModels(value);[m
       return;[m
     }[m
 [m
[36m@@ -838,6 +904,7 @@[m [mconst EditChannel = (props) => {[m
       }[m
 [m
 [m
[32m+[m[32m      // 更新基础模型选项供参考，但不自动填充到 Select 组件[m
       let localModels = [];[m
       switch (value) {[m
         case 2:[m
[36m@@ -878,9 +945,6 @@[m [mconst EditChannel = (props) => {[m
           localModels = getChannelModels(value);[m
           break;[m
       }[m
[31m-      if (inputs.models.length === 0 || inputs.type !== value) { // Only update models if type changes or models are empty[m
[31m-        setInputs((inputs) => ({ ...inputs, models: localModels }));[m
[31m-      }[m
       setBasicModels(localModels);[m
     }[m
   };[m
[36m@@ -969,12 +1033,14 @@[m [mconst EditChannel = (props) => {[m
 [m
       // 初始化模型原始映射关系[m
       const mapping = parseModelMapping(data.model_mapping);[m
[31m-      if (mapping) {[m
[32m+[m[32m      if (mapping && Object.keys(mapping).length > 0) {[m
         const initialMapping = {};[m
         // 根据当前的模型映射和模型列表，建立原始映射关系[m
[31m-        Object.entries(mapping).forEach(([key, value]) => {[m
[31m-          if (data.models.includes(key)) {[m
[31m-            initialMapping[key] = value;[m
[32m+[m[32m        Object.entries(mapping).forEach(([displayName, originalName]) => {[m
[32m+[m[32m          const displayNameTrimmed = displayName.trim();[m
[32m+[m[32m          const originalNameTrimmed = originalName?.trim();[m
[32m+[m[32m          if (displayNameTrimmed && originalNameTrimmed && data.models.includes(displayNameTrimmed)) {[m
[32m+[m[32m            initialMapping[displayNameTrimmed] = originalNameTrimmed;[m
           }[m
         });[m
         setModelOriginalMapping(initialMapping);[m
[36m@@ -1063,7 +1129,6 @@[m [mconst EditChannel = (props) => {[m
       setModelOriginalMapping({});[m
       let localModels = getChannelModels(originInputs.type); // Use originInputs.type for initial state[m
       setBasicModels(localModels);[m
[31m-      setInputs((inputs) => ({ ...inputs, models: localModels }));[m
       setComponentResetKey(prev => prev + 1);[m
     }[m
   }, [props.editingChannel.id]);[m
[36m@@ -1386,9 +1451,9 @@[m [mconst EditChannel = (props) => {[m
               {t('切换为单密钥模式')}[m
             </Button>[m
           </div>[m
[31m-          [m
[31m-          <div style={{ [m
[31m-            maxHeight: '50vh', [m
[32m+[m
[32m+[m[32m          <div style={{[m
[32m+[m[32m            maxHeight: '50vh',[m
             overflowY: 'auto',[m
             border: '1px solid var(--semi-color-border)',[m
             borderRadius: '6px',[m
[36m@@ -1397,9 +1462,9 @@[m [mconst EditChannel = (props) => {[m
           }}>[m
             {keyList.map((key, index) => ([m
               <div key={index} style={{ display: 'flex', marginBottom: '5px', alignItems: 'center' }} className="key-input-item">[m
[31m-                <Typography.Text [m
[31m-                  style={{ [m
[31m-                    minWidth: '30px', [m
[32m+[m[32m                <Typography.Text[m
[32m+[m[32m                  style={{[m
[32m+[m[32m                    minWidth: '30px',[m
                     textAlign: 'center',[m
                     color: 'var(--semi-color-text-2)',[m
                     fontSize: 12,[m
[36m@@ -1422,7 +1487,7 @@[m [mconst EditChannel = (props) => {[m
                   theme="borderless"[m
                   size="small"[m
                   onClick={() => copyKey(key)}[m
[31m-                  style={{ [m
[32m+[m[32m                  style={{[m
                     marginLeft: '4px',[m
                     minWidth: '28px',[m
                     opacity: key && key.trim() ? 1 : 0.3[m
[36m@@ -1435,7 +1500,7 @@[m [mconst EditChannel = (props) => {[m
                   theme="borderless"[m
                   size="small"[m
                   onClick={() => removeKeyInput(index)}[m
[31m-                  style={{ [m
[32m+[m[32m                  style={{[m
                     marginLeft: '8px',[m
                     minWidth: '28px',[m
                     opacity: keyList.length <= 1 ? 0.3 : 1[m
[36m@@ -1445,7 +1510,7 @@[m [mconst EditChannel = (props) => {[m
               </div>[m
             ))}[m
           </div>[m
[31m-          [m
[32m+[m
           <div style={{ marginTop: 12, display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>[m
             <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>[m
               <Button[m
[36m@@ -1470,7 +1535,7 @@[m [mconst EditChannel = (props) => {[m
               {t('总计: {{count}} 个密钥', { count: keyList.length })}[m
             </Typography.Text>[m
           </div>[m
[31m-          [m
[32m+[m
           <Typography.Text type="tertiary" style={{ fontSize: 12, marginTop: 8, display: 'block' }}>[m
             {t('💡 提示: 输入逗号或回车可快速添加新密钥')}[m
           </Typography.Text>[m
[36m@@ -1508,7 +1573,7 @@[m [mconst EditChannel = (props) => {[m
             </Button>[m
           )}[m
         </div>[m
[31m-        [m
[32m+[m
         <Input[m
           ref={singleKeyInputRef}[m
           name='key'[m
[36m@@ -1581,7 +1646,7 @@[m [mconst EditChannel = (props) => {[m
             </Space>[m
           }[m
         />[m
[31m-        [m
[32m+[m
         {/* 清空按钮 */}[m
         {inputs.key && ([m
           <Button[m
[36m@@ -1973,7 +2038,9 @@[m [mconst EditChannel = (props) => {[m
             filter[m
             searchPosition='dropdown'[m
             onChange={(value) => {[m
[31m-              handleInputChange('models', value);[m
[32m+[m[32m              // 对模型列表进行去重处理（区分大小写）[m
[32m+[m[32m              const deduplicatedModels = [...new Set(value.filter(model => model && typeof model === 'string' && model.trim() !== ''))];[m
[32m+[m[32m              handleInputChange('models', deduplicatedModels);[m
             }}[m
             value={inputs.models}[m
             autoComplete='new-password'[m
[36m@@ -2105,6 +2172,7 @@[m [mconst EditChannel = (props) => {[m
             key={`model-mapping-${componentResetKey}`}[m
             value={originalModelMapping || inputs.model_mapping}[m
             onChange={(value) => handleInputChange('model_mapping', value)}[m
[32m+[m[32m            onRealtimeChange={syncModelMappingToModels}[m
             placeholder={t('此项可选，用于修改请求体中的模型名称')}[m
           />[m
           <div style={{ marginTop: 8 }}>[m
